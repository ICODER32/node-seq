<div>
  {{#each this}}
    <div class="card">
        <h3 class="card-header"></h3>
        <section class="comment card-body">
            <div class="text">
              <p>  {{comment_text}} </p>
            </div>
            <div class="meta">
            <p>   -{{user.username}}, {{format_date created_at}} {{format_time created_at}} </p>
            </div>
        </section>
    </div>
  {{/each}}
</div>


