<div class="card">
    <div class="card-header" style="background-color:#474973;" >
        <h4><a class="link text-light" href="/dashboard/edit/{{id}}">{{title}}</a></h4>
        <h6 class="mb-2 text-light">{{format_date created_at}} {{format_time created_at}} </h6>
    </div>
        <div class="card-body">
        <p class="card-text">{{content}}</p>
    </div>
</div>